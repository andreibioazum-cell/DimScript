struct Player
    var name: string = "Hero"
    var hp: i32 = 100
end

func damage_player(p: Player, amount: i32)
    p.hp = p.hp - amount
    print("Player HP is now: " .. p.hp)
end

func main_logic()
    var x: i32 = 10
    if x > 5 then
        print("Logic works! Value: " .. x)
    end
end
